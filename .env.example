# Network interface for BACnet (optional; BAC0 can auto-pick)
# Example: LOCAL_INTERFACE=192.168.10.25/24
# LOCAL_INTERFACE=

# Database (SQLite default)
DB_PATH=data/bacnet_topology.db

# Poller defaults (used if not specified in extraction_map.csv)
DEFAULT_INTERVAL_SEC=300
